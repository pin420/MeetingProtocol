<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/app_bar_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="ifRoom"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/searchOption_mans"
            android:title="@string/search_option_mans"
            app:showAsAction="never"
            android:checked="true" />

        <item
            android:id="@+id/searchOption_protocol"
            android:title="@string/search_option_protocol"
            app:showAsAction="never" />

        <item
            android:id="@+id/searchOption_all"
            android:title="@string/search_option_all"
            app:showAsAction="never" />
    </group>

    <item
        android:id="@+id/about_app"
        android:title="@string/About"
        app:showAsAction="never" />

</menu>